import { formatAmount } from './formatAmount';
import { formatDateNumber } from './formatDateNumber';
import {
  formatPhoneNumber,
  formatPhoneNumberReplace,
} from './formatPhoneNumber';
import { formatNumber } from './formatNumber';

export {
  formatAmount,
  formatDateNumber,
  formatPhoneNumber,
  formatPhoneNumberReplace,
  formatNumber,
};
